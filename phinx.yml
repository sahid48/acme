paths:
    migrations: %%PHINX_CONFIG_DIR%%/migrations

environments:
    default_migration_table: phinxlog
    default_database: development
    production:
        adapter: pgsql
        host: localhost
        name: acme
        user: postgres
        pass: 'secret'
        port: 5432
        charset: utf8

    development:
        adapter: pgsql
        host: localhost
        name: acme
        user: postgres
        pass: 'secret'
        port: 5432
        charset: utf8

    testing:
      adapter: pgsql
      host: localhost
      name: acme
      user: postgres
      pass: 'secret'
      port: 5432
      charset: utf8
